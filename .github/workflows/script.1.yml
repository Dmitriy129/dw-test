name: script 1
on:
  pull_request:
    types:
      - opened
      - reopened
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - 
        name: set config path
        run: |
          echo "PR_BOT_CONFIG_PATH='./bot_config'" >> "$GITHUB_ENV"
      - 
        name: Check out code
        uses: actions/checkout@v2
      - 
        name: Run script
        uses: Dmitriy129/moodle-github-sync-1@0.0.2

      - run: echo "🍏 This job's status is ${{ job.status }}."